Template or Demo of a typescript monorepo for a serverless application in aws.

- Use NPM workspaces to manage packages.
- Re-map local package imports to lookup locations so that "go to definition works".
