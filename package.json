{
  "name": "@demo/project",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "workspaces": [
    "packages/config",
    "packages/frontend"
  ]
}
